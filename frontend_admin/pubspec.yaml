name: frontend_admin
description: Dashboard Web Admin per AutAnalysis
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  file_picker: ^8.0.0

flutter:
  uses-material-design: true
